# Fastlane Fastfile
# https://docs.fastlane.tools

default_platform(:ios)

platform :ios do
  desc "Run all tests"
  lane :test do
    scan(
      project: "WhisperBoard.xcodeproj",
      scheme: "WhisperBoard",
      device: "iPhone 15",
      clean: true
    )
  end

  desc "Build the app"
  lane :build do
    gym(
      project: "WhisperBoard.xcodeproj",
      scheme: "WhisperBoard",
      export_method: "development",
      output_directory: "build",
      build_path: "build"
    )
  end

  desc "Deploy to TestFlight (Beta)"
  lane :beta do
    # App Store Connect API key from CI secrets — no Apple ID password, no match/certs repo.
    api_key = app_store_connect_api_key(
      key_id: ENV["APP_STORE_CONNECT_API_KEY_ID"],
      issuer_id: ENV["APP_STORE_CONNECT_API_ISSUER_ID"],
      key_content: ENV["APP_STORE_CONNECT_API_KEY_CONTENT"],
      is_key_content_base64: false,
      in_house: false
    )

    # Monotonic build number from the CI run. The .xcodeproj is regenerated by XcodeGen
    # every run, so the committed value is irrelevant — we set it explicitly here.
    increment_build_number(
      xcodeproj: "WhisperBoard.xcodeproj",
      build_number: ENV["GITHUB_RUN_NUMBER"] || "1"
    )

    # Build + sign with Xcode cloud-managed AUTOMATIC signing. -allowProvisioningUpdates
    # lets Xcode create/download the distribution cert + profiles for both the app and the
    # keyboard extension using the API key above (fastlane forwards it automatically).
    gym(
      project: "WhisperBoard.xcodeproj",
      scheme: "WhisperBoard",
      export_method: "app-store",
      output_directory: "build",
      build_path: "build",
      include_bitcode: false,
      include_symbols: true,
      xcargs: "-allowProvisioningUpdates",
      export_options: {
        signingStyle: "automatic"
      }
    )

    # Upload to TestFlight via the API key. skip_waiting=true → don't block CI on Apple
    # processing; the build shows up in TestFlight a few minutes after upload.
    upload_to_testflight(
      api_key: api_key,
      app_identifier: "com.captainsos.whisperboard",
      ipa: "build/WhisperBoard.ipa",
      skip_waiting_for_build_processing: true,
      distribute_external: false,
      notify_external_testers: false
    )

    clean_build_artifacts

    UI.success("🎉 Successfully deployed to TestFlight!")
  end

  desc "Deploy to App Store (Production)"
  lane :release do
    # Run tests first
    test

    # Increment version number
    increment_version_number(
      xcodeproj: "WhisperBoard.xcodeproj"
    )

    # Build and sign the app
    gym(
      project: "WhisperBoard.xcodeproj",
      scheme: "WhisperBoard",
      export_method: "app-store",
      output_directory: "build"
    )

    # Upload to App Store
    deliver(
      ipa: "build/WhisperBoard.ipa",
      force: true,
      skip_metadata: false,
      skip_screenshots: false,
      submit_for_review: false
    )

    UI.success("🎉 Successfully deployed to App Store Connect!")
  end

  desc "Sync code signing certificates"
  lane :sync_certs do
    match(
      type: "appstore",
      app_identifier: "com.captainsos.whisperboard",
      readonly: true
    )
  end

  desc "Setup code signing for new machine"
  lane :setup_certs do
    match(
      type: "development",
      app_identifier: "com.captainsos.whisperboard"
    )
    match(
      type: "appstore",
      app_identifier: "com.captainsos.whisperboard"
    )
  end

  desc "Take screenshots for App Store"
  lane :screenshots do
    capture_screenshots(
      project: "WhisperBoard.xcodeproj",
      scheme: "WhisperBoardUITests",
      output_directory: "fastlane/screenshots"
    )
  end

end
